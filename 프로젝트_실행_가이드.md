# 인보이스 생성기 실행 가이드

## 🚀 프로젝트 구조

```
/workspace/
├── src/                    # React 프론트엔드
│   ├── components/         # React 컴포넌트
│   ├── services/          # API 서비스
│   └── utils/             # 유틸리티 함수
├── backend/               # Spring Boot 백엔드
│   ├── src/main/java/     # Java 소스코드
│   ├── src/main/resources/ # 설정 파일
│   ├── pom.xml            # Maven 의존성
│   └── run.sh             # 백엔드 실행 스크립트
└── package.json           # React 의존성
```

## 📋 실행 전 요구사항

### 프론트엔드 (React)
- Node.js 16+ 
- npm 또는 yarn

### 백엔드 (Spring Boot)
- Java 17+
- Maven 3.6+

## 🏃‍♂️ 실행 방법

### 1. 백엔드 서버 실행

```bash
# 1. Java 및 Maven 설치 (Ubuntu/Debian)
sudo apt update
sudo apt install -y openjdk-17-jdk maven

# 2. 백엔드 디렉토리로 이동
cd backend

# 3. 실행 스크립트 사용 (추천)
./run.sh

# 또는 직접 실행
mvn clean compile
mvn spring-boot:run
```

**백엔드 서버 주소**: `http://localhost:8080`

### 2. 프론트엔드 서버 실행

```bash
# 1. 프로젝트 루트 디렉토리로 이동
cd /workspace

# 2. 의존성 설치 (처음 실행 시)
npm install

# 3. 개발 서버 시작
npm start
```

**프론트엔드 서버 주소**: `http://localhost:3000`

## 🔧 기능 설명

### 백엔드 API 기능
- ✅ **완전한 CRUD**: 인보이스 생성, 조회, 수정, 삭제
- ✅ **데이터베이스 연동**: H2(개발용), MySQL, PostgreSQL 지원
- ✅ **데이터 검증**: Jakarta Validation으로 입력값 검증
- ✅ **검색 기능**: 회사명, 고객명, 날짜 범위별 검색
- ✅ **CORS 설정**: React 프론트엔드와 연동
- ✅ **자동 계산**: 세금 및 총액 자동 계산

### 프론트엔드 기능
- ✅ **백엔드 연동**: 자동으로 백엔드 서버와 연결
- ✅ **오프라인 모드**: 백엔드 연결 실패 시 로컬 스토리지 사용
- ✅ **인보이스 저장**: 백엔드 데이터베이스에 저장
- ✅ **PDF 생성**: 기존 PDF 다운로드 기능 유지
- ✅ **반응형 디자인**: 모바일 및 데스크톱 지원

## 📊 데이터베이스 설정

### H2 Database (기본 - 개발용)
- 메모리 데이터베이스, 재시작 시 데이터 초기화
- H2 콘솔: `http://localhost:8080/h2-console`
- JDBC URL: `jdbc:h2:mem:testdb`
- Username: `sa`, Password: (비워두기)

### MySQL 사용 시
```sql
CREATE DATABASE invoice_db;
CREATE USER 'invoice_user'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON invoice_db.* TO 'invoice_user'@'localhost';
FLUSH PRIVILEGES;
```

```bash
# MySQL 프로파일로 실행
mvn spring-boot:run -Dspring-boot.run.profiles=mysql
```

### PostgreSQL 사용 시
```sql
CREATE DATABASE invoice_db;
CREATE USER invoice_user WITH PASSWORD 'password';
GRANT ALL PRIVILEGES ON DATABASE invoice_db TO invoice_user;
```

```bash
# PostgreSQL 프로파일로 실행
mvn spring-boot:run -Dspring-boot.run.profiles=postgresql
```

## 🔍 API 엔드포인트

| 메소드 | URL | 설명 |
|--------|-----|------|
| GET | `/api/invoices/all` | 모든 인보이스 조회 |
| GET | `/api/invoices/{id}` | ID로 인보이스 조회 |
| POST | `/api/invoices` | 새 인보이스 생성 |
| PUT | `/api/invoices/{id}` | 인보이스 수정 |
| DELETE | `/api/invoices/{id}` | 인보이스 삭제 |
| GET | `/api/invoices/search/company?name=회사명` | 회사명으로 검색 |
| GET | `/api/invoices/search/client?name=고객명` | 고객명으로 검색 |
| GET | `/api/invoices/recent` | 최근 인보이스 10개 |
| GET | `/api/invoices/health` | API 상태 확인 |

## 🎯 사용 방법

1. **백엔드 서버 실행** → `http://localhost:8080`에서 실행 확인
2. **프론트엔드 서버 실행** → `http://localhost:3000`에서 앱 접속
3. **인보이스 작성** → 회사 정보, 고객 정보, 상품 정보 입력
4. **인보이스 저장** → "인보이스 저장" 버튼으로 데이터베이스에 저장
5. **PDF 다운로드** → "PDF 다운로드" 버튼으로 PDF 파일 생성
6. **새 인보이스** → "새 인보이스" 버튼으로 새 문서 시작

## ⚠️ 문제 해결

### npm start가 느린 경우
- **원인**: React 개발 서버의 초기 빌드 시간
- **해결방법**: 
  - 첫 실행 후 Hot Reload로 빠른 업데이트
  - `npm run build`로 프로덕션 빌드 생성

### 백엔드 연결 실패
- **확인사항**: 
  - Java 17+ 설치 확인: `java -version`
  - Maven 설치 확인: `mvn -version`
  - 포트 8080 사용 중인지 확인: `lsof -i :8080`
- **해결방법**: 백엔드 서버 재시작

### CORS 오류
- **원인**: 프론트엔드(3000)와 백엔드(8080) 포트가 다름
- **해결방법**: 이미 CORS 설정 완료, 브라우저 캐시 삭제 후 재시도

## 🎉 완료된 기능

✅ **Spring Boot 백엔드 구축**
✅ **H2/MySQL/PostgreSQL 데이터베이스 연동**  
✅ **JPA 엔티티 및 Repository 구현**
✅ **REST API 컨트롤러 구현**
✅ **React 프론트엔드와 백엔드 연동**
✅ **CORS 설정**
✅ **인보이스 저장/조회 기능**
✅ **오프라인 모드 지원**
✅ **반응형 UI 개선**

이제 완전한 풀스택 인보이스 생성기가 완성되었습니다! 🎊